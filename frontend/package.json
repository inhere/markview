{
  "name": "markview-web",
  "version": "1.0.0",
  "description": "Frontend assets for MarkView",
  "type": "module",
  "scripts": {
    "build": "rm -rf ./dist && bun run bun.build.ts",
    "dev": "WATCH=true && rm -rf ./dist && bun run bun.build.ts"
  },
  "dependencies": {
    "mermaid": "^10.9.0",
    "highlight.js": "^11.9.0"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
